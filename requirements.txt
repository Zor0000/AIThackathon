# VeriSure runtime stack — Python implementation of the supplied build reference.

# HR dashboard and webhook/API service
streamlit>=1.49,<2
fastapi>=0.141,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.20,<1

# Configuration, validation, HTTP, resilience, and structured audit logging
python-dotenv>=1.1,<2
pydantic>=2.11,<3
httpx>=0.28,<1
tenacity>=9,<10
structlog>=25,<26

# WhatsApp messaging and webhook signature validation
twilio>=9.11,<10

# Stateful AI orchestration and structured extraction
langgraph>=1.2,<2
langgraph-checkpoint-postgres>=3,<4
openai>=1.0,<3

# PostgreSQL persistence, migrations, and asynchronous retries/follow-ups
SQLAlchemy>=2.0,<3
psycopg[binary,pool]>=3.3,<4
alembic>=1.14,<2
redis[hiredis]>=8.1,<9
rq>=2.11,<3
